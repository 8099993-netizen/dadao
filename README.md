# Dadao landing page

Static bilingual landing page for `https://fbhao.xyz/`.

## Local preview

Serve the repository root with any static HTTP server, then open `index.html` through that server.

## Deployment

The site is designed for GitHub Pages from the `main` branch root. `CNAME` keeps the custom domain set to `fbhao.xyz`.
